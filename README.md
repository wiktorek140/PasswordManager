Short launch instruction. 

Modify .env to use local database. Create in that database "passwordwallet"

Execute next part:
composer install
npm install
npm install
php artisan migrate
php artisan serve

